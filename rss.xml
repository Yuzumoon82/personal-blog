<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Yuzumoon82</title><description>Yuzumoon82 的博客</description><link>https://yuzumoon82.github.io/personal-blog/</link><language>zh_CN</language><item><title>Mizuki 项目学习计划</title><link>https://yuzumoon82.github.io/personal-blog/posts/mizuki%E9%A1%B9%E7%9B%AE%E5%AD%A6%E4%B9%A0%E8%AE%A1%E5%88%92/</link><guid isPermaLink="true">https://yuzumoon82.github.io/personal-blog/posts/mizuki%E9%A1%B9%E7%9B%AE%E5%AD%A6%E4%B9%A0%E8%AE%A1%E5%88%92/</guid><description>从零到上线自己的博客——Mizuki 开源 Astro 静态博客模板学习路径</description><pubDate>Sat, 26 Jul 2025 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;Mizuki 项目学习计划 —— 从零到上线自己的博客&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;Mizuki 是一个开源的 Astro 静态博客模板。你的目标：看懂它 → 改它 → 把它变成你自己的博客。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;hr /&gt;
&lt;h2&gt;〇、先认识这个项目&lt;/h2&gt;
&lt;h3&gt;Mizuki 是什么&lt;/h3&gt;
&lt;p&gt;一个&lt;strong&gt;静态博客网站&lt;/strong&gt;。写 Markdown 文章 → 自动生成 HTML 页面 → 部署到网上就能访问。&lt;/p&gt;
&lt;h3&gt;技术栈一览&lt;/h3&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;技术&lt;/th&gt;
&lt;th&gt;干什么的&lt;/th&gt;
&lt;th&gt;你需要学到什么程度&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;Astro&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;框架，把 Markdown + 组件 → HTML 页面&lt;/td&gt;
&lt;td&gt;会用 .astro 文件，理解 frontmatter 和 template&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;Tailwind CSS&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;写样式的，用 class 名控制外观&lt;/td&gt;
&lt;td&gt;会查文档，找到你想要的 class 名&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;Svelte&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;做交互组件（音乐播放器、搜索等）&lt;/td&gt;
&lt;td&gt;先跳过，只看不改&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;TypeScript&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;带类型的 JavaScript&lt;/td&gt;
&lt;td&gt;能读懂就行，不需要写&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;Markdown/MDX&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;写文章的格式&lt;/td&gt;
&lt;td&gt;会用 # 标题、列表、链接&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h3&gt;目录结构速览&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;Mizuki/
├── astro.config.mjs          ← 网站配置（插件、域名、SEO）
├── src/
│   ├── pages/                ← ★ 页面：首页、关于、文章列表...
│   ├── components/           ← ★ 组件：按钮、卡片、导航栏...
│   │   ├── atoms/            ←   最小组件（Button, Icon, Badge）
│   │   ├── widgets/          ←   大组件（音乐播放器、评论）
│   │   └── layout/           ←   页面骨架（header, footer, sidebar）
│   ├── layouts/Layout.astro  ← ★ 全局布局（所有页面都套这个壳）
│   ├── config/               ← ★ 配置文件（改这个就能改网站内容）
│   ├── content/posts/        ← ★ 博客文章（Markdown 文件）
│   ├── styles/               ← 样式文件
│   └── data/                 ← 数据文件（项目列表、朋友列表等）
├── public/                   ← 静态资源（图片、字体）
└── package.json              ← 项目依赖和脚本命令
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;h2&gt;第一阶段：跑起来（Day 1）&lt;/h2&gt;
&lt;h3&gt;目标：能在本地浏览器看到这个网站&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Step 1：安装依赖&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;cd /Users/dduo/WebstormProjects/Mizuki

# 如果没有 pnpm，先装它
npm install -g pnpm

# 装项目依赖
pnpm install
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Step 2：启动开发服务器&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;pnpm dev
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;浏览器打开 &lt;code&gt;http://localhost:4321&lt;/code&gt; —— 你应该能看到一个完整的博客网站。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Step 3：改一行字，验证热更新&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;打开 &lt;code&gt;src/config/site.ts&lt;/code&gt;（或 &lt;code&gt;src/config/&lt;/code&gt; 下的配置文件），找到网站标题，改一个字，保存。浏览器自动刷新——这说明&lt;strong&gt;热更新&lt;/strong&gt;在工作。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;今天学到什么&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;pnpm 是包管理器，&lt;code&gt;pnpm install&lt;/code&gt; = 下载项目需要的所有代码&lt;/li&gt;
&lt;li&gt;&lt;code&gt;pnpm dev&lt;/code&gt; = 启动开发服务器，&quot;dev&quot; = development&lt;/li&gt;
&lt;li&gt;Astro 的热更新：改代码 → 保存 → 浏览器自动刷新，不用手动 F5&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;第二阶段：改内容，不动代码（Day 2-3）&lt;/h2&gt;
&lt;h3&gt;Day 2：改配置 —— 把网站变成你的&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;目标&lt;/strong&gt;：不用写一行 HTML/CSS，只改配置文件，让这个网站变成&quot;你的&quot;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;任务 1：改网站基本信息&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;打开 &lt;code&gt;src/config/&lt;/code&gt; 目录，找到这些配置文件，逐行看，改掉能看懂的：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;网站标题、描述&lt;/li&gt;
&lt;li&gt;你的名字、头像链接&lt;/li&gt;
&lt;li&gt;导航栏菜单项&lt;/li&gt;
&lt;li&gt;社交链接（GitHub、邮箱）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;每改一处保存，看浏览器变化。这是最快建立信心的方式。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;任务 2：改首页 Hero 区&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;找首页的配置（通常在 config 文件或 &lt;code&gt;src/pages/index.astro&lt;/code&gt; 的 frontmatter），改：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;大标题&lt;/li&gt;
&lt;li&gt;副标题&lt;/li&gt;
&lt;li&gt;按钮文字&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;任务 3：理解 Tailwind CSS 的 class&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;打开 &lt;code&gt;src/pages/index.astro&lt;/code&gt;，你会看到很多像 &lt;code&gt;class=&quot;text-2xl font-bold text-center&quot;&lt;/code&gt; 这样的代码。不用背，记住两个网站：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://tailwindcss.com/docs&quot;&gt;Tailwind CSS 文档&lt;/a&gt; —— 查任何 class 的含义&lt;/li&gt;
&lt;li&gt;改一个 class，保存，看效果。比如把 &lt;code&gt;text-2xl&lt;/code&gt; 改成 &lt;code&gt;text-4xl&lt;/code&gt;，字体会变大&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;今天学到什么&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;配置驱动：这个项目的很多内容不用改代码，改配置就行&lt;/li&gt;
&lt;li&gt;Tailwind CSS：用 class 控制样式，不用写 CSS 文件&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 3：写第一篇文章&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;目标&lt;/strong&gt;：在博客上发布一篇你自己的文章。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;任务 1：找到文章目录&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ls src/content/posts/
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;你会看到一些 &lt;code&gt;.md&lt;/code&gt; 或 &lt;code&gt;.mdx&lt;/code&gt; 文件。这些就是博客文章。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;任务 2：新建一篇文章&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;pnpm new-post
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;按提示输入标题。会在 &lt;code&gt;src/content/posts/&lt;/code&gt; 下生成一个新文件。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;任务 3：写 Markdown&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;打开你新建的文件，顶部是 frontmatter（用 &lt;code&gt;---&lt;/code&gt; 包起来的部分）：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;---
title: 我的第一篇文章
date: 2026-07-25
description: 这是我用 Mizuki 写的第一篇博客
tags: [前端, 学习]
---

## 今天学到了什么

今天我开始学习 Astro...

## 代码示例

​```javascript
console.log(&apos;hello world&apos;);
​```
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;保存 → 浏览器自动刷新 → 首页应该能看到你的新文章。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;今天学到什么&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Markdown frontmatter：文章标题、日期、标签在 &lt;code&gt;---&lt;/code&gt; 之间&lt;/li&gt;
&lt;li&gt;Markdown 语法：&lt;code&gt;#&lt;/code&gt; = 标题，&lt;code&gt;##&lt;/code&gt; = 二级标题，``` 包起来 = 代码块&lt;/li&gt;
&lt;li&gt;Astro 的内容集合：&lt;code&gt;src/content/posts/&lt;/code&gt; 下的 .md 文件自动变成博客文章&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;第三阶段：改样式，理解组件（Day 4-6）&lt;/h2&gt;
&lt;h3&gt;Day 4：理解 Astro 组件&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;目标&lt;/strong&gt;：能看懂 &lt;code&gt;.astro&lt;/code&gt; 文件的结构。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;.astro 文件的两段式结构&lt;/strong&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;---
// ↑↑↑ 上面这段叫 frontmatter（脚本区）
// 在这里写 JavaScript/TypeScript
// 可以 import 其他组件、定义变量、获取数据
import Layout from &apos;../layouts/Layout.astro&apos;;
const title = &apos;我的页面&apos;;
---

&amp;lt;!-- ↓↓↓ 下面这段叫 template（模板区） --&amp;gt;
&amp;lt;!-- 在这里写 HTML --&amp;gt;
&amp;lt;Layout&amp;gt;
  &amp;lt;h1&amp;gt;{title}&amp;lt;/h1&amp;gt;
  &amp;lt;p&amp;gt;这是一个 Astro 组件&amp;lt;/p&amp;gt;
&amp;lt;/Layout&amp;gt;

&amp;lt;style&amp;gt;
  /* 样式只影响当前组件，不会泄露到其他组件 */
  h1 { color: red; }
&amp;lt;/style&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;练习&lt;/strong&gt;：打开 &lt;code&gt;src/pages/about.astro&lt;/code&gt;，对照上面的结构看懂它。改一下 &lt;code&gt;&amp;lt;h1&amp;gt;&lt;/code&gt; 里的文字，保存看效果。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;任务&lt;/strong&gt;：找一个 atom 组件（如 &lt;code&gt;src/components/atoms/Button/Button.astro&lt;/code&gt;），逐行读完。原子组件通常很短（20-50 行），是最容易看懂的入口。&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 5：理解布局和插槽&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;目标&lt;/strong&gt;：理解&quot;所有页面套同一个壳&quot;是怎么实现的。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;核心概念&lt;/strong&gt;：&lt;code&gt;Layout.astro&lt;/code&gt;（&lt;code&gt;src/layouts/Layout.astro&lt;/code&gt;）。&lt;/p&gt;
&lt;p&gt;打开它，你会看到类似这样的结构：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;html&amp;gt;
  &amp;lt;head&amp;gt;...&amp;lt;/head&amp;gt;
  &amp;lt;body&amp;gt;
    &amp;lt;Header /&amp;gt;         ← 导航栏，每个页面都一样
    &amp;lt;main&amp;gt;
      &amp;lt;slot /&amp;gt;         ← ★ 插槽：每个页面的不同内容放在这里
    &amp;lt;/main&amp;gt;
    &amp;lt;Footer /&amp;gt;         ← 底部，每个页面都一样
  &amp;lt;/body&amp;gt;
&amp;lt;/html&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;&amp;lt;slot /&amp;gt;&lt;/code&gt; 是关键——它是一个占位符。当你在 &lt;code&gt;about.astro&lt;/code&gt; 里写：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;Layout title=&quot;关于我&quot;&amp;gt;
  &amp;lt;h1&amp;gt;这是关于页面&amp;lt;/h1&amp;gt;     ← 这段内容替换 &amp;lt;slot /&amp;gt;
&amp;lt;/Layout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;练习&lt;/strong&gt;：打开 &lt;code&gt;src/layouts/Layout.astro&lt;/code&gt;，找到 &lt;code&gt;&amp;lt;slot /&amp;gt;&lt;/code&gt; 的位置。然后打开 &lt;code&gt;src/pages/about.astro&lt;/code&gt;，看看它怎么用 &lt;code&gt;&amp;lt;Layout&amp;gt;&lt;/code&gt; 包裹自己的内容。&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 6：改一个组件 —— 把导航栏改成你自己的&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;目标&lt;/strong&gt;：找到导航栏组件，改菜单项。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;定位导航栏&lt;/strong&gt;：&lt;/p&gt;
&lt;p&gt;导航栏通常在 &lt;code&gt;src/components/layout/&lt;/code&gt; 或 &lt;code&gt;src/components/organisms/&lt;/code&gt; 下，找文件名含 &lt;code&gt;Header&lt;/code&gt;、&lt;code&gt;Nav&lt;/code&gt;、&lt;code&gt;Navbar&lt;/code&gt; 的文件。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;练习&lt;/strong&gt;：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;打开导航栏组件&lt;/li&gt;
&lt;li&gt;找到菜单项（通常是 &lt;code&gt;&amp;lt;a&amp;gt;&lt;/code&gt; 标签列表）&lt;/li&gt;
&lt;li&gt;改一个菜单名（比如 &quot;关于&quot; 改成 &quot;关于我&quot;）&lt;/li&gt;
&lt;li&gt;给&quot;关于我&quot;加一个高亮样式（加 &lt;code&gt;class=&quot;text-blue-500&quot;&lt;/code&gt; 或 &lt;code&gt;class=&quot;font-bold&quot;&lt;/code&gt;）&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;今天学到什么&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;组件的复用：导航栏写在 Header 组件里一次，所有页面都自动有&lt;/li&gt;
&lt;li&gt;Tailwind 的 class 命名规律：&lt;code&gt;text-&lt;/code&gt; = 文字颜色，&lt;code&gt;font-&lt;/code&gt; = 字体粗细，&lt;code&gt;bg-&lt;/code&gt; = 背景色&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;第四阶段：加功能，真正动手（Day 7-8）&lt;/h2&gt;
&lt;h3&gt;Day 7：加一个新页面&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;目标&lt;/strong&gt;：从零新建一个页面，加到导航栏里。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Step 1：创建页面文件&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;在 &lt;code&gt;src/pages/&lt;/code&gt; 下新建 &lt;code&gt;learning.astro&lt;/code&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;---
import Layout from &apos;../layouts/Layout.astro&apos;;
---

&amp;lt;Layout title=&quot;我的学习记录&quot;&amp;gt;
  &amp;lt;main class=&quot;max-w-2xl mx-auto py-20&quot;&amp;gt;
    &amp;lt;h1 class=&quot;text-3xl font-bold&quot;&amp;gt;📚 我的前端学习记录&amp;lt;/h1&amp;gt;
    &amp;lt;p class=&quot;mt-4 text-gray-600&quot;&amp;gt;这里记录我每天学到的东西。&amp;lt;/p&amp;gt;
  &amp;lt;/main&amp;gt;
&amp;lt;/Layout&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;保存，访问 &lt;code&gt;http://localhost:4321/learning&lt;/code&gt; —— 你的新页面在。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Step 2：加到导航栏&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;找到导航栏组件，在菜单列表里加一个 &lt;code&gt;&amp;lt;a href=&quot;/learning&quot;&amp;gt;学习记录&amp;lt;/a&amp;gt;&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;今天学到什么&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Astro 的文件路由：&lt;code&gt;src/pages/xxx.astro&lt;/code&gt; → 自动映射到 &lt;code&gt;/xxx&lt;/code&gt; 这个 URL&lt;/li&gt;
&lt;li&gt;加页面只需要两步：建文件 + 加导航链接&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 8：读懂一个 Svelte 交互组件（只读不改）&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;目标&lt;/strong&gt;：理解项目中 &lt;code&gt;.svelte&lt;/code&gt; 文件是干什么的。&lt;/p&gt;
&lt;p&gt;Svelte 和 Astro 的区别：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;.astro&lt;/code&gt; 文件：在&lt;strong&gt;服务器端&lt;/strong&gt;运行，生成静态 HTML，没有交互&lt;/li&gt;
&lt;li&gt;&lt;code&gt;.svelte&lt;/code&gt; 文件：在&lt;strong&gt;浏览器端&lt;/strong&gt;运行，可以做交互（点击、输入、动画）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;练习&lt;/strong&gt;：打开 &lt;code&gt;src/components/widgets/music-player/MusicPlayer.svelte&lt;/code&gt;，不用全看懂，找到这三样东西：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;&amp;lt;script&amp;gt;&lt;/code&gt; 标签里的 &lt;code&gt;let&lt;/code&gt; 变量 —— 这就是组件的&quot;状态&quot;（比如 &lt;code&gt;let isPlaying = false&lt;/code&gt;）&lt;/li&gt;
&lt;li&gt;模板里的 &lt;code&gt;{#if ...}&lt;/code&gt; 和 &lt;code&gt;{变量}&lt;/code&gt; —— Svelte 的模板语法&lt;/li&gt;
&lt;li&gt;&lt;code&gt;on:click={...}&lt;/code&gt; —— 事件监听&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;今天学到什么&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;.astro = 静态内容，构建时生成 HTML&lt;/li&gt;
&lt;li&gt;.svelte = 交互内容，用户在浏览器里点按钮时响应&lt;/li&gt;
&lt;li&gt;不需要现在就学会写 Svelte，知道它们在项目里的角色就够了&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;第五阶段：部署上线（Day 9-10）&lt;/h2&gt;
&lt;h3&gt;Day 9：构建 + 部署到 GitHub Pages&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;目标&lt;/strong&gt;：你的博客有一个人人可以访问的网址。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Step 1：构建&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;pnpm build
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;构建产物在 &lt;code&gt;dist/&lt;/code&gt; 目录下——这就是你的整个网站，纯 HTML+CSS+JS，可以部署到任何静态托管服务。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Step 2：推到自己新建的 GitHub 仓库&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# 在 GitHub 上新建一个仓库，比如 mizuki-blog
git remote add myblog https://github.com/你的用户名/mizuki-blog.git
git push myblog main
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Step 3：用 Vercel 部署（比 GitHub Pages 更简单）&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;访问 &lt;a href=&quot;https://vercel.com&quot;&gt;vercel.com&lt;/a&gt;，用 GitHub 登录&lt;/li&gt;
&lt;li&gt;点 &quot;New Project&quot; → 选择你的仓库&lt;/li&gt;
&lt;li&gt;不用改任何配置，点 Deploy&lt;/li&gt;
&lt;li&gt;等 2 分钟，拿到一个 &lt;code&gt;xxx.vercel.app&lt;/code&gt; 的网址&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;今天学到什么&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;pnpm build&lt;/code&gt; = 生产构建，把所有代码编译成浏览器能直接读的 HTML/CSS/JS&lt;/li&gt;
&lt;li&gt;Vercel 自动检测到 Astro 项目，零配置部署&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 10：写 3 篇文章 + 完善博客&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;目标&lt;/strong&gt;：你的博客有内容可以给人看了。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;写 3 篇文章&lt;/strong&gt;：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&quot;我是怎么开始学前端的&quot; —— 自我介绍&lt;/li&gt;
&lt;li&gt;&quot;前端学习第一周总结&quot; —— 记录你的学习过程&lt;/li&gt;
&lt;li&gt;&quot;Mizuki 博客搭建记录&quot; —— 记录你是怎么做这个博客的&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;完善细节&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;把默认的头像换成你自己的&lt;/li&gt;
&lt;li&gt;把默认的项目卡片换成你自己的项目链接&lt;/li&gt;
&lt;li&gt;把 README.md 改成你自己的项目介绍&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;学习方法&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;原则&lt;/th&gt;
&lt;th&gt;说明&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;先改配置文件，再动组件代码&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;配置文件是最安全的修改入口，改不坏&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;改一个 class → 保存 → 看效果&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;Tailwind 的 class 不用背，试出来的记得最牢&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;遇到看不懂的代码，先跳过&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;这个项目有几百个文件，不需要全看懂&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;每天只吃透一个文件&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;比如今天就看懂 Button.astro，明天看 Header.astro&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;不要怕改坏&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;改坏了 &lt;code&gt;git checkout -- .&lt;/code&gt; 一键恢复&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;hr /&gt;
&lt;h2&gt;按照前端学习计划，这个项目对应什么水平&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;前端学习计划的阶段&lt;/th&gt;
&lt;th&gt;Mizuki 中的对应&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Day 1-2（CSS 实战）&lt;/td&gt;
&lt;td&gt;改 Tailwind class，观察效果&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Day 3-4（JS 原生）&lt;/td&gt;
&lt;td&gt;看懂 .astro 的 frontmatter 脚本区&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Day 5-6（第一个项目）&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;Mizuki 就是你的第一个项目&lt;/strong&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Day 9-10（Vue 入门）&lt;/td&gt;
&lt;td&gt;Svelte 和 Vue 概念相通：组件、响应式、插槽&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Week 3（组件库）&lt;/td&gt;
&lt;td&gt;Mizuki 的 atoms/ 就是一套组件库&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;hr /&gt;
&lt;blockquote&gt;
&lt;p&gt;10 天后，你应该有一个自己的博客、3 篇文章、一个可访问的网址。重要的是——你知道改哪里能改什么。&lt;/p&gt;
&lt;/blockquote&gt;
</content:encoded></item><item><title>Markdown 入门教程</title><link>https://yuzumoon82.github.io/personal-blog/posts/markdown-tutorial/</link><guid isPermaLink="true">https://yuzumoon82.github.io/personal-blog/posts/markdown-tutorial/</guid><description>Markdown 语法完整示例，涵盖块级元素、行内元素及扩展语法。</description><pubDate>Mon, 20 Jan 2025 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;Markdown 入门教程&lt;/h1&gt;
&lt;p&gt;本文演示如何使用 Markdown 编写文章，涵盖核心语法和 GFM 扩展。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;#%E5%9D%97%E7%BA%A7%E5%85%83%E7%B4%A0&quot;&gt;块级元素&lt;/a&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;#%E6%AE%B5%E8%90%BD%E4%B8%8E%E6%8D%A2%E8%A1%8C&quot;&gt;段落与换行&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E6%A0%87%E9%A2%98&quot;&gt;标题&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E5%BC%95%E7%94%A8&quot;&gt;引用&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E5%88%97%E8%A1%A8&quot;&gt;列表&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E4%BB%A3%E7%A0%81%E5%9D%97&quot;&gt;代码块&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E5%88%86%E9%9A%94%E7%BA%BF&quot;&gt;分隔线&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E8%A1%A8%E6%A0%BC&quot;&gt;表格&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E8%A1%8C%E5%86%85%E5%85%83%E7%B4%A0&quot;&gt;行内元素&lt;/a&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;#%E9%93%BE%E6%8E%A5&quot;&gt;链接&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E5%BC%BA%E8%B0%83&quot;&gt;强调&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E8%A1%8C%E5%86%85%E4%BB%A3%E7%A0%81&quot;&gt;行内代码&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E5%9B%BE%E7%89%87&quot;&gt;图片&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E5%88%A0%E9%99%A4%E7%BA%BF&quot;&gt;删除线&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E5%85%B6%E4%BB%96&quot;&gt;其他&lt;/a&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;#%E8%87%AA%E5%8A%A8%E9%93%BE%E6%8E%A5&quot;&gt;自动链接&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E5%8F%8D%E6%96%9C%E6%9D%A0%E8%BD%AC%E4%B9%89&quot;&gt;反斜杠转义&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;#%E5%86%85%E8%81%94-html&quot;&gt;内联 HTML&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;块级元素&lt;/h2&gt;
&lt;h3&gt;段落与换行&lt;/h3&gt;
&lt;h4&gt;段落&lt;/h4&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;p&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;一个或多个空行分隔段落。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;这是第一段。

这是第二段。
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;p&gt;这是第一段。&lt;/p&gt;
&lt;p&gt;这是第二段。&lt;/p&gt;
&lt;h4&gt;换行&lt;/h4&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;br /&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;在行尾加&lt;strong&gt;两个或更多空格&lt;/strong&gt;实现换行。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;这一行不会  
换行。
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;p&gt;这一行不会&lt;br /&gt;
换行。&lt;/p&gt;
&lt;h3&gt;标题&lt;/h3&gt;
&lt;h4&gt;Setext 风格&lt;/h4&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;h1&amp;gt;&lt;/code&gt;, &lt;code&gt;&amp;lt;h2&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;用等号 &lt;code&gt;=&lt;/code&gt; 表示一级标题，短横线 &lt;code&gt;-&lt;/code&gt; 表示二级标题。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;这是一级标题
=============
这是二级标题
-------------
&lt;/code&gt;&lt;/pre&gt;
&lt;h4&gt;atx 风格&lt;/h4&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;h1&amp;gt;&lt;/code&gt; ~ &lt;code&gt;&amp;lt;h6&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;行首使用 1-6 个 &lt;code&gt;#&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# 一级标题
## 二级标题
###### 六级标题
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;h1&gt;一级标题&lt;/h1&gt;
&lt;h2&gt;二级标题&lt;/h2&gt;
&lt;h6&gt;六级标题&lt;/h6&gt;
&lt;h3&gt;引用&lt;/h3&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;blockquote&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;使用 &lt;code&gt;&amp;gt;&lt;/code&gt; 符号。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;gt; 这是一段引用。
&amp;gt; 
&amp;gt; 这是第二段引用。
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;这是一段引用。&lt;/p&gt;
&lt;p&gt;这是第二段引用。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;嵌套引用：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;gt; 第一层引用
&amp;gt; &amp;gt; 嵌套引用
&amp;gt; 回到第一层
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;第一层引用&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;嵌套引用
回到第一层&lt;/p&gt;
&lt;/blockquote&gt;
&lt;/blockquote&gt;
&lt;h3&gt;列表&lt;/h3&gt;
&lt;h4&gt;无序列表&lt;/h4&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;ul&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;使用 &lt;code&gt;*&lt;/code&gt;、&lt;code&gt;+&lt;/code&gt; 或 &lt;code&gt;-&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;* 红色
* 绿色
* 蓝色
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;红色&lt;/li&gt;
&lt;li&gt;绿色&lt;/li&gt;
&lt;li&gt;蓝色&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;有序列表&lt;/h4&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;ol&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;使用数字加点。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;1. 第一项
2. 第二项
3. 第三项
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;第一项&lt;/li&gt;
&lt;li&gt;第二项&lt;/li&gt;
&lt;li&gt;第三项&lt;/li&gt;
&lt;/ol&gt;
&lt;h4&gt;嵌套列表&lt;/h4&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;* A
  * A1
  * A2
* B
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;A
&lt;ul&gt;
&lt;li&gt;A1&lt;/li&gt;
&lt;li&gt;A2&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;B&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;代码块&lt;/h3&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;pre&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;用三个反引号包裹，可选语言标识启用语法高亮。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;```javascript
function hello() {
  console.log(&quot;Hello World!&quot;);
}
```
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;function hello() {
  console.log(&quot;Hello World!&quot;);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;分隔线&lt;/h3&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;hr /&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;三个或更多 &lt;code&gt;*&lt;/code&gt;、&lt;code&gt;-&lt;/code&gt; 或 &lt;code&gt;_&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;---
***
___
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;表格&lt;/h3&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;table&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;用 &lt;code&gt;|&lt;/code&gt; 分隔列，&lt;code&gt;-&lt;/code&gt; 分隔表头，&lt;code&gt;:&lt;/code&gt; 控制对齐。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;| 左对齐 | 居中 | 右对齐 |
|:-------|:----:|-------:|
| aaa    | bbb  | ccc    |
| ddd    | eee  | fff    |
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;左对齐&lt;/th&gt;
&lt;th&gt;居中&lt;/th&gt;
&lt;th&gt;右对齐&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;aaa&lt;/td&gt;
&lt;td&gt;bbb&lt;/td&gt;
&lt;td&gt;ccc&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;ddd&lt;/td&gt;
&lt;td&gt;eee&lt;/td&gt;
&lt;td&gt;fff&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2&gt;行内元素&lt;/h2&gt;
&lt;h3&gt;链接&lt;/h3&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;a&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;格式：&lt;code&gt;[链接文字](URL &quot;标题&quot;)&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;[示例链接](https://example.com &quot;可选标题&quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;a href=&quot;https://example.com&quot;&gt;示例链接&lt;/a&gt;&lt;/p&gt;
&lt;h3&gt;强调&lt;/h3&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;em&amp;gt;&lt;/code&gt;, &lt;code&gt;&amp;lt;strong&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;*斜体*
**粗体**
***粗斜体***
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;em&gt;斜体&lt;/em&gt; &lt;strong&gt;粗体&lt;/strong&gt; &lt;em&gt;&lt;strong&gt;粗斜体&lt;/strong&gt;&lt;/em&gt;&lt;/p&gt;
&lt;h3&gt;行内代码&lt;/h3&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;code&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;使用反引号包裹。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;使用 `console.log()` 函数。
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：使用 &lt;code&gt;console.log()&lt;/code&gt; 函数。&lt;/p&gt;
&lt;h3&gt;图片&lt;/h3&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;img /&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;格式：&lt;code&gt;![替代文字](图片URL &quot;可选标题&quot;)&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;![风景](https://s2.loli.net/2024/08/20/5fszgXeOxmL3Wdv.webp)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;https://s2.loli.net/2024/08/20/5fszgXeOxmL3Wdv.webp&quot; alt=&quot;风景&quot; /&gt;&lt;/p&gt;
&lt;h3&gt;删除线&lt;/h3&gt;
&lt;p&gt;HTML 标签：&lt;code&gt;&amp;lt;del&amp;gt;&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;~~这是错误的内容~~
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;s&gt;这是错误的内容&lt;/s&gt;&lt;/p&gt;
&lt;h2&gt;其他&lt;/h2&gt;
&lt;h3&gt;自动链接&lt;/h3&gt;
&lt;p&gt;用尖括号包裹 URL 或邮箱。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;https://example.com&amp;gt;
&amp;lt;email@example.com&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;a href=&quot;https://example.com&quot;&gt;https://example.com&lt;/a&gt;&lt;/p&gt;
&lt;h3&gt;反斜杠转义&lt;/h3&gt;
&lt;p&gt;用 &lt;code&gt;\&lt;/code&gt; 转义特殊字符。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;\*不是斜体\*
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：*不是斜体*&lt;/p&gt;
&lt;p&gt;可转义字符：&lt;code&gt;\&lt;/code&gt; &lt;code&gt;*&lt;/code&gt; &lt;code&gt;_&lt;/code&gt; &lt;code&gt;{}&lt;/code&gt; &lt;code&gt;[]&lt;/code&gt; &lt;code&gt;()&lt;/code&gt; &lt;code&gt;#&lt;/code&gt; &lt;code&gt;+&lt;/code&gt; &lt;code&gt;-&lt;/code&gt; &lt;code&gt;.&lt;/code&gt; &lt;code&gt;!&lt;/code&gt;&lt;/p&gt;
&lt;h2&gt;内联 HTML&lt;/h2&gt;
&lt;p&gt;Markdown 不支持的标记，可以直接写 HTML。&lt;/p&gt;
&lt;p&gt;代码：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;这是普通段落。

&amp;lt;table&amp;gt;
    &amp;lt;tr&amp;gt;&amp;lt;td&amp;gt;表格内容&amp;lt;/td&amp;gt;&amp;lt;/tr&amp;gt;
&amp;lt;/table&amp;gt;

这是另一个段落。
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;效果：&lt;/p&gt;
&lt;p&gt;这是普通段落。&lt;/p&gt;
&lt;p&gt;&amp;lt;table&amp;gt;
&amp;lt;tr&amp;gt;&amp;lt;td&amp;gt;表格内容&amp;lt;/td&amp;gt;&amp;lt;/tr&amp;gt;
&amp;lt;/table&amp;gt;&lt;/p&gt;
&lt;p&gt;这是另一个段落。&lt;/p&gt;
</content:encoded></item><item><title>Mizuki Music Player 实现详解</title><link>https://yuzumoon82.github.io/personal-blog/posts/mizuki-musicplayer%E5%AE%9E%E7%8E%B0%E8%AF%A6%E8%A7%A3/</link><guid isPermaLink="true">https://yuzumoon82.github.io/personal-blog/posts/mizuki-musicplayer%E5%AE%9E%E7%8E%B0%E8%AF%A6%E8%A7%A3/</guid><description>从零拆解音乐播放器组件——Svelte 与 Vue 对照，面向新手</description><pubDate>Fri, 25 Jul 2025 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;Mizuki Music Player 实现详解&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;从零拆解一个完整的前端组件——音乐播放器。面向 Vue 新手，每一步都标注 Svelte 和 Vue 的对应写法。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;hr /&gt;
&lt;h2&gt;〇、先理解三个基础概念（Svelte ↔ Vue 对照表）&lt;/h2&gt;
&lt;p&gt;你看代码时会遇到 Svelte 语法，它们和 Vue 的对应关系如下：&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;概念&lt;/th&gt;
&lt;th&gt;Svelte 写法&lt;/th&gt;
&lt;th&gt;Vue 3 写法&lt;/th&gt;
&lt;th&gt;作用&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;组件状态&lt;/td&gt;
&lt;td&gt;&lt;code&gt;let state = ...&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;const state = ref(...)&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;存数据，数据变了自动更新DOM&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;父传子&lt;/td&gt;
&lt;td&gt;&lt;code&gt;&amp;lt;Child prop={value} /&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;&amp;lt;Child :prop=&quot;value&quot; /&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;把数据传给子组件&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;子传父&lt;/td&gt;
&lt;td&gt;&lt;code&gt;&amp;lt;Child onclick={fn} /&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;&amp;lt;Child @click=&quot;fn&quot; /&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;子组件通知父组件&quot;发生了某件事&quot;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;组件挂载&lt;/td&gt;
&lt;td&gt;&lt;code&gt;onMount(() =&amp;gt; {...})&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;onMounted(() =&amp;gt; {...})&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;组件出现在页面上时执行&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;组件销毁&lt;/td&gt;
&lt;td&gt;&lt;code&gt;onDestroy(() =&amp;gt; {...})&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;onUnmounted(() =&amp;gt; {...})&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;组件从页面移除时执行（清理定时器、取消订阅）&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;模板条件&lt;/td&gt;
&lt;td&gt;&lt;code&gt;{#if condition}...{/if}&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;&amp;lt;div v-if=&quot;condition&quot;&amp;gt;...&amp;lt;/div&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;条件渲染&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;模板列表&lt;/td&gt;
&lt;td&gt;&lt;code&gt;{#each list as item}...{/each}&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;&amp;lt;div v-for=&quot;item in list&quot;&amp;gt;...&amp;lt;/div&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;列表渲染&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;动态class&lt;/td&gt;
&lt;td&gt;&lt;code&gt;class:active={isActive}&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;:class=&quot;{ active: isActive }&quot;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;条件class&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;插槽&lt;/td&gt;
&lt;td&gt;&lt;code&gt;&amp;lt;slot /&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;&amp;lt;slot /&amp;gt;&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;父组件往子组件里插内容&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;hr /&gt;
&lt;h2&gt;一、项目文件地图（你只需要看这 12 个文件）&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;src/
├── config/musicConfig.ts                    ← ① 配置文件：歌单ID、音乐源
├── stores/musicPlayerStore.ts               ← ② ★ 核心大脑：所有状态+所有操作
│
├── components/widgets/music-player/
│   ├── MusicPlayer.svelte                   ← ③ ★ 主组件：组装一切，连接Store
│   ├── constants.ts                         ← ④ 常量：默认歌曲、本地歌单
│   ├── types.ts                             ← ⑤ 类型定义：Song对象长什么样
│   │
│   ├── organisms/                           ← ⑥ 有机体：由多个分子组成的大块
│   │   ├── PlayerBar.svelte                 ←    展开状态面板（全功能）
│   │   ├── MiniPlayer.svelte                ←    收起状态面板（迷你）
│   │   └── Playlist.svelte                  ←    歌单列表弹窗
│   │
│   ├── molecules/                           ← ⑦ 分子：由原子组成的中间块
│   │   ├── TrackDisplay.svelte              ←    歌曲封面+歌名+歌手
│   │   ├── PlayerControls.svelte            ←    播放/上一首/下一首按钮组
│   │   ├── ProgressControl.svelte           ←    进度条
│   │   └── VolumeControl.svelte             ←    音量滑块+静音按钮
│   │
│   └── atoms/                               ← ⑧ 原子：最小的独立UI单元
│       ├── CoverImage.svelte                ←    封面图（可旋转、可点击）
│       ├── TrackInfo.svelte                 ←    歌名+歌手文字
│       ├── PlayButton.svelte                ←    播放/暂停按钮
│       ├── PrevButton.svelte                ←    上一首按钮
│       ├── NextButton.svelte                ←    下一首按钮
│       ├── ModeButton.svelte                ←    随机/循环模式按钮
│       ├── ProgressBar.svelte               ←    进度条滑块
│       ├── VolumeButton.svelte              ←    音量图标按钮
│       └── VolumeSlider.svelte              ←    音量滑动条
│
└── components/control/
    └── MusicFabButton.svelte                ← ⑨ 悬浮按钮：点击展开/收起播放器
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;组件层级&lt;/strong&gt;：MusicPlayer → PlayerBar/MiniPlayer → TrackDisplay/PlayerControls → CoverImage/PlayButton...&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;二、从最简单的文件开始看&lt;/h2&gt;
&lt;h3&gt;2.1 配置文件 &lt;code&gt;musicConfig.ts&lt;/code&gt;（你唯一需要改的文件）&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;// src/config/musicConfig.ts
export const musicPlayerConfig = {
    enable: true,                    // 总开关，false=整个播放器不加载
    mode: &quot;local&quot;,                   // &quot;local&quot;=用本地写死的歌单 / &quot;meting&quot;=连API获取
    // 下面这些只在 mode=&quot;meting&quot; 时生效
    id: &quot;14164869977&quot;,              // 网易云歌单ID（去网易云网页版URL里找）
    server: &quot;netease&quot;,              // 音乐源：netease / tencent / kugou
    type: &quot;playlist&quot;,               // &quot;playlist&quot;=歌单 / &quot;song&quot;=单曲 / &quot;album&quot;=专辑
    showFloatingPlayer: true,       // 显示右下角悬浮播放器
    floatingEntryMode: &quot;fab&quot;,       // &quot;fab&quot;=集成到悬浮按钮 / &quot;default&quot;=独立悬浮
};
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;你需要知道的事&lt;/strong&gt;：整个音乐播放器的行为由这个文件控制。换歌单只改 &lt;code&gt;id&lt;/code&gt;，关播放器只改 &lt;code&gt;enable: false&lt;/code&gt;。&lt;/p&gt;
&lt;h3&gt;2.2 类型定义 &lt;code&gt;types.ts&lt;/code&gt;（Song 对象长什么样）&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;// 这是我们自己定义的数据结构，一首歌就是一个Song对象
export interface Song {
    id: number;        // 歌曲ID
    title: string;     // 歌名，如&quot;晴天&quot;
    artist: string;    // 歌手，如&quot;周杰伦&quot;
    cover: string;     // 封面图URL
    url: string;       // ★ 最关键：音乐文件的URL，浏览器要能直接播放
    duration: number;  // 时长（秒）
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;2.3 常量 &lt;code&gt;constants.ts&lt;/code&gt;（默认值和本地歌单）&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;// 默认歌曲（歌单还没加载时先显示这个）
export const DEFAULT_SONG: Song = {
    id: 0,
    title: &quot;未知歌曲&quot;,
    artist: &quot;未知歌手&quot;,
    cover: &quot;&quot;,
    url: &quot;&quot;,
    duration: 0,
};

// 本地模式用的歌单（mode=&quot;local&quot;时直接用这个）
export const LOCAL_PLAYLIST: Song[] = [
    // 你可以把mp3文件放public目录下，然后在这里写路径
    { id: 1, title: &quot;示例歌曲&quot;, artist: &quot;示例歌手&quot;,
      cover: &quot;/assets/music/cover.jpg&quot;,
      url: &quot;/assets/music/song.mp3&quot;,
      duration: 0 },
];

// 歌曲加载失败后等5秒自动跳下一首
export const SKIP_ERROR_DELAY = 5000;

// 音量存localStorage的key名
export const STORAGE_KEY_VOLUME = &quot;mizuki-music-volume&quot;;
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;h2&gt;三、核心大脑 &lt;code&gt;musicPlayerStore.ts&lt;/code&gt;（591行，最重要）&lt;/h2&gt;
&lt;p&gt;这是整个播放器的灵魂。你先理解下面这张图，再逐段看代码。&lt;/p&gt;
&lt;h3&gt;3.1 整体结构（Vue 等价物：Pinia Store）&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;┌─────────────────────────────────────────────────────┐
│                  musicPlayerStore                    │
│                                                     │
│  state = {                    ← 唯一的数据源         │
│    currentSong: Song,         当前播哪首             │
│    playlist: Song[],          整个歌单               │
│    currentIndex: number,      播到第几首             │
│    isPlaying: boolean,        正在播放？             │
│    currentTime: number,       当前秒数               │
│    duration: number,          总时长                 │
│    volume: number,            音量 0-1               │
│    isShuffled: boolean,       随机？                 │
│    isRepeating: 0|1|2,        0=不循环 1=单曲 2=列表 │
│    isExpanded: boolean,       面板展开？             │
│    isHidden: boolean,         面板隐藏？             │
│  }                                                  │
│                                                     │
│  audio = new Audio()          浏览器原生播放器        │
│                                                     │
│  listeners = Set&amp;lt;fn&amp;gt;          订阅者列表              │
│                                                     │
│  方法（你调这些来控制播放器）：                        │
│  toggle() / play() / pause() / next() / prev()       │
│  setVolume(n) / toggleMute() / seek(秒)              │
│  toggleShuffle() / toggleRepeat()                    │
│  toggleExpanded() / toggleHidden()                   │
│                                                     │
│  subscribe(fn) → 返回 unsubscribe 函数               │
│  broadcastState() → 通知所有订阅者                    │
└─────────────────────────────────────────────────────┘
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;3.2 初始化：&lt;code&gt;initialize()&lt;/code&gt; —— 启动时做了什么&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;// musicPlayerStore.ts L105-120
async initialize(): Promise&amp;lt;void&amp;gt; {
    // ① 防止重复初始化（服务器端不初始化，因为没有window）
    if (typeof window === &quot;undefined&quot; || this.isInitialized) return;
    this.isInitialized = true;

    // ② 如果配置里关了播放器，直接返回
    if (!musicPlayerConfig.enable) return;

    // ③ ★ 创建浏览器原生 Audio 对象
    //    Audio 是浏览器内置的，不需要安装任何库
    //    new Audio() 就等于你在HTML里写 &amp;lt;audio&amp;gt;&amp;lt;/audio&amp;gt;
    this.audio = new Audio();

    // ④ 给 Audio 绑定事件监听（play、pause、ended、error...）
    this.setupAudioListeners();

    // ⑤ 从 localStorage 恢复上次的音量
    this.loadVolumeFromStorage();

    // ⑥ 注册用户交互处理（浏览器禁止自动播放，需等用户点一下页面）
    this.registerInteractionHandler();

    // ⑦ ★ 加载歌单（要么调API，要么用本地歌单）
    await this.loadPlaylist();
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;这一步做完后&lt;/strong&gt;：&lt;code&gt;audio&lt;/code&gt; 对象创建好了，歌单加载了，第一首歌的 URL 设好了（但还没开始播——浏览器禁止自动播放）。&lt;/p&gt;
&lt;h3&gt;3.3 绑定 Audio 事件：&lt;code&gt;setupAudioListeners()&lt;/code&gt;&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;// musicPlayerStore.ts L122-163
private setupAudioListeners(): void {
    // ★ Audio 对象会自己触发这些事件，我们只需要&quot;听&quot;它们

    // 事件1：开始播放了 → 更新 isPlaying = true → 通知所有组件
    this.audio.addEventListener(&quot;play&quot;, () =&amp;gt; {
        this.state.isPlaying = true;
        this.broadcastState();  // ★ 每次状态变化都通知订阅者
    });

    // 事件2：暂停了 → 更新 isPlaying = false → 通知
    this.audio.addEventListener(&quot;pause&quot;, () =&amp;gt; {
        this.state.isPlaying = false;
        this.broadcastState();
    });

    // 事件3：播放进度更新（浏览器每250ms触发一次）→ 更新进度条
    this.audio.addEventListener(&quot;timeupdate&quot;, () =&amp;gt; {
        this.state.currentTime = this.audio.currentTime;
        this.broadcastState();
    });

    // 事件4：播完了 → 自动下一首（或单曲循环）
    this.audio.addEventListener(&quot;ended&quot;, () =&amp;gt; this.handleAudioEnded());

    // 事件5：加载出错了 → 显示错误，5秒后跳下一首
    this.audio.addEventListener(&quot;error&quot;, () =&amp;gt; this.handleAudioError());

    // 事件6：数据加载完成 → 拿到歌曲总时长（duration）
    this.audio.addEventListener(&quot;loadeddata&quot;, () =&amp;gt; this.handleAudioLoaded());

    // 事件7：开始加载 → 显示loading状态
    this.audio.addEventListener(&quot;loadstart&quot;, () =&amp;gt; {
        this.state.isLoading = true;
        this.broadcastState();
    });
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;你需要知道的事&lt;/strong&gt;：&lt;code&gt;audio&lt;/code&gt; 是浏览器原生对象，它会自动触发 &lt;code&gt;play&lt;/code&gt;、&lt;code&gt;pause&lt;/code&gt;、&lt;code&gt;ended&lt;/code&gt; 这些事件。我们要做的只是在事件处理函数里&lt;strong&gt;更新 state 然后 &lt;code&gt;broadcastState()&lt;/code&gt;&lt;/strong&gt;。所有组件都会自动收到新状态。&lt;/p&gt;
&lt;h3&gt;3.4 加载歌单：&lt;code&gt;loadPlaylist()&lt;/code&gt;&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;// musicPlayerStore.ts L249-268
private async loadPlaylist(): Promise&amp;lt;void&amp;gt; {
    const mode = musicPlayerConfig.mode;  // &quot;local&quot; 或 &quot;meting&quot;

    if (mode === &quot;meting&quot;) {
        // ★ 在线模式：调Meting API获取歌单
        //   把配置里的 server/type/id 拼到API URL里
        //   例如：https://api.example.com/api?server=netease&amp;amp;type=playlist&amp;amp;id=14164869977
        await this.fetchMetingPlaylist(api, server, type, id);
    } else {
        // ★ 本地模式：直接用 constants.ts 里写死的 LOCAL_PLAYLIST
        this.loadLocalPlaylist();
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Meting API 是什么&lt;/strong&gt;：一个开源的音乐API，能根据网易云/QQ音乐的歌单ID返回歌曲列表（歌名、歌手、封面、mp3地址）。Mizuki 项目自己部署了一个 Meting API 服务，URL 配置在 &lt;code&gt;musicConfig.meting_api&lt;/code&gt; 里。&lt;/p&gt;
&lt;h3&gt;3.5 加载一首歌：&lt;code&gt;loadSong()&lt;/code&gt;&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;// musicPlayerStore.ts L350-371
private loadSong(song: Song, autoPlay = true): void {
    if (!song?.url) return;  // 没有URL就跳过

    // ★ 把歌曲URL设给 Audio 对象
    //   这就等于 &amp;lt;audio src=&quot;https://music.xxx.com/song.mp3&quot;&amp;gt;&amp;lt;/audio&amp;gt;
    this.audio.src = getAssetPath(song.url);

    // ★ 调 load() 让浏览器开始加载音频数据
    //   加载完成后 Audio 会自动触发 &quot;loadeddata&quot; 事件
    this.audio.load();

    // 更新当前歌曲信息
    this.state.currentSong = { ...song };
    this.state.willAutoPlay = autoPlay;  // 加载完是否自动播放

    this.broadcastState();
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;3.6 通知所有组件：&lt;code&gt;broadcastState()&lt;/code&gt; —— 最关键的方法&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;// musicPlayerStore.ts L560-575
private broadcastState(): void {
    // ★ 深拷贝一份 state，防止组件意外修改原始数据
    const snapshot = this.createSnapshot();

    // ★ 方式1：通知所有 Svelte 组件（通过 subscribe 注册的回调）
    for (const listener of this.listeners) {
        listener(snapshot);
    }

    // ★ 方式2：同时用浏览器原生 CustomEvent 通知非Svelte代码
    //   这样任何JS代码都可以通过 addEventListener(&quot;music-sidebar:state&quot;) 收到通知
    window.dispatchEvent(
        new CustomEvent(&quot;music-sidebar:state&quot;, { detail: snapshot })
    );
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;broadcastState()&lt;/code&gt; 就是整个播放器的核心机制&lt;/strong&gt;。任何操作（播放、暂停、切歌、调音量）最后都会走到这个方法——它保证了所有组件看到的数据永远一致。&lt;/p&gt;
&lt;h3&gt;3.7 核心操作实现&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;播放/暂停&lt;/strong&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// L388-397
toggle(): void {
    if (this.state.isPlaying) {
        this.audio.pause();  // ★ 调浏览器原生方法 → 触发 &quot;pause&quot; 事件
    } else {
        this.audio.play();   // ★ 调浏览器原生方法 → 触发 &quot;play&quot; 事件
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;下一首（随机模式支持）&lt;/strong&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// L413-435
next(autoPlay = true): void {
    let newIndex: number;
    if (this.state.isShuffled) {
        // ★ 随机模式：在0到歌单长度之间随机选一个，但排除当前这首
        do {
            newIndex = Math.floor(Math.random() * this.state.playlist.length);
        } while (newIndex === this.state.currentIndex &amp;amp;&amp;amp; this.state.playlist.length &amp;gt; 1);
    } else {
        // ★ 顺序模式：下一首，到底了回到第一首
        newIndex = this.state.currentIndex &amp;lt; this.state.playlist.length - 1
            ? this.state.currentIndex + 1 : 0;
    }
    this.state.currentIndex = newIndex;
    this.loadSong(this.state.playlist[newIndex], autoPlay);
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;歌曲播完自动处理&lt;/strong&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// L165-175
private handleAudioEnded(): void {
    if (this.state.isRepeating === 1) {
        // 单曲循环 → 回到开头重新播放
        this.audio.currentTime = 0;
        this.audio.play();
    } else {
        // 否则 → 自动下一首（列表循环到底会回第一首，因为 next() 里处理的）
        this.next(true);
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;歌曲加载失败&lt;/strong&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;// L177-187
private handleAudioError(): void {
    this.state.isLoading = false;
    this.showError(&quot;无法播放&quot;);  // 弹出红色错误提示，3秒后自动消失
    if (this.state.playlist.length &amp;gt; 1) {
        setTimeout(() =&amp;gt; this.next(true), 5000);  // 5秒后自动跳过
    }
}
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;h2&gt;四、组件层 —— 从外到内看&lt;/h2&gt;
&lt;h3&gt;4.1 MusicPlayer.svelte（主组件，相当于 Vue 的页面级组件）&lt;/h3&gt;
&lt;p&gt;这是整个播放器的&lt;strong&gt;唯一入口组件&lt;/strong&gt;，放在 &lt;code&gt;Layout.astro&lt;/code&gt; 里，所有页面都加载。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;它的三件事&lt;/strong&gt;：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;① 挂载时订阅 Store&lt;/strong&gt;（Vue 等价：onMounted 里 watch store）：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;script&amp;gt;
  // 拿到当前状态的快照
  let state = musicPlayerStore.getState();

  onMount(() =&amp;gt; {
    // ★ 订阅：Store 每次 broadcastState() 都会调这个回调
    //   Vue 等价写法：watch(() =&amp;gt; store.state, (newVal) =&amp;gt; { state = newVal })
    unsubscribe = musicPlayerStore.subscribe((nextState) =&amp;gt; {
      state = nextState;  // Svelte 检测到赋值会自动更新所有用到 state 的 DOM
    });

    // ★ 启动：连API拿歌单、创建Audio对象
    musicPlayerStore.initialize();
  });

  onDestroy(() =&amp;gt; {
    unsubscribe?.();           // 取消订阅 → 避免内存泄漏
    musicPlayerStore.destroy(); // 清理 Audio 对象 → 释放浏览器资源
  });
&amp;lt;/script&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;② 事件处理 —— 自己不做逻辑，只调 Store 方法&lt;/strong&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;script&amp;gt;
  // ★ 组件里只做一件事：调用 Store 的方法。不自己改状态。
  function togglePlay()   { musicPlayerStore.toggle(); }
  function prev()         { musicPlayerStore.prev(); }
  function next()         { musicPlayerStore.next(); }
  function toggleShuffle(){ musicPlayerStore.toggleShuffle(); }
  function toggleRepeat() { musicPlayerStore.toggleRepeat(); }

  // ★ 进度条拖拽稍微复杂一点：需要根据鼠标位置计算百分比
  function setProgress(event: MouseEvent) {
    const rect = event.currentTarget.getBoundingClientRect();
    const percent = (event.clientX - rect.left) / rect.width;
    musicPlayerStore.setProgress(percent);  // 最后也是调Store方法
  }

  // ★ 音量拖拽类似：监听 pointermove 事件，实时计算位置
  function startVolumeDrag(event: PointerEvent) {
    const updateVolume = (clientX) =&amp;gt; {
      const rect = slider.getBoundingClientRect();
      const percent = Math.max(0, Math.min(1, (clientX - rect.left) / rect.width));
      musicPlayerStore.setVolume(percent);
    };
    updateVolume(event.clientX);
    // 监听拖动和释放事件，拖完清理监听器
  }
&amp;lt;/script&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;③ 模板 —— 条件渲染不同的UI状态&lt;/strong&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;!-- 错误提示：state.showError 为 true 时显示红色提示框 --&amp;gt;
{#if state.showError}
  &amp;lt;div class=&quot;fixed bottom-20 right-4 bg-red-500 text-white ...&quot;&amp;gt;
    {state.errorMessage}
    &amp;lt;button onclick={hideError}&amp;gt;×&amp;lt;/button&amp;gt;
  &amp;lt;/div&amp;gt;
{/if}

&amp;lt;!-- fab模式：点击悬浮按钮 → 展开面板，Svelte 的 fly 过渡动画 --&amp;gt;
{#if useFabEntry}
  {#if state.isExpanded}
    &amp;lt;div transition:fly={{ y: 16, duration: 280 }}&amp;gt;
      &amp;lt;FabMusicPanel /&amp;gt;
    &amp;lt;/div&amp;gt;
  {/if}
{/if}

&amp;lt;!-- 展开面板：全部功能 --&amp;gt;
&amp;lt;PlayerBar
  song={state.currentSong}      ← 父传子：把当前歌曲传给子组件
  isPlaying={state.isPlaying}   ← 父传子：播放状态
  onPlayClick={togglePlay}      ← 子传父：子组件点击播放 → 调父组件的togglePlay
  onNextClick={() =&amp;gt; next()}    ← 子传父：下一首
  onProgressClick={setProgress} ← 子传父：点击进度条
/&amp;gt;

&amp;lt;!-- 收起面板：迷你模式 --&amp;gt;
&amp;lt;MiniPlayer
  song={state.currentSong}
  isPlaying={state.isPlaying}
  onCoverClick={togglePlay}     ← 点击封面=播放/暂停
  onInfoClick={toggleExpanded}  ← 点击歌名区域=展开
/&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;4.2 PlayerBar.svelte（展开面板）&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;这和 Vue 的纯展示组件完全一样&lt;/strong&gt;——只接收 props，不访问 Store：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;script&amp;gt;
  // ★ 定义 Props 接口（Vue: defineProps&amp;lt;{...}&amp;gt;()）
  interface Props {
    song: Song;                 // 数据：歌曲信息
    isPlaying: boolean;         // 数据：播放状态
    onPlayClick: () =&amp;gt; void;    // 事件：点击播放按钮时通知父组件
    onNextClick: () =&amp;gt; void;    // 事件：点击下一首时通知父组件
    onProgressClick: (e: MouseEvent) =&amp;gt; void;  // 事件：点击进度条
    // ...
  }
  const { song, isPlaying, onPlayClick, onNextClick, ... } = $props();
&amp;lt;/script&amp;gt;

&amp;lt;div class=&quot;expanded-player ...&quot;&amp;gt;
  &amp;lt;!-- 歌曲展示区：封面+歌名+歌手 --&amp;gt;
  &amp;lt;TrackDisplay {song} {isPlaying} /&amp;gt;

  &amp;lt;!-- 进度条 --&amp;gt;
  &amp;lt;ProgressControl {currentTime} {duration} onclick={onProgressClick} /&amp;gt;

  &amp;lt;!-- 控制按钮组：随机、上一首、播放/暂停、下一首、循环 --&amp;gt;
  &amp;lt;PlayerControls {isPlaying} onPlayClick={onPlayClick} onNextClick={onNextClick} /&amp;gt;

  &amp;lt;!-- 音量控制：音量图标+滑块 --&amp;gt;
  &amp;lt;VolumeControl {volume} {isMuted} onclick={onVolumeButtonClick} /&amp;gt;
&amp;lt;/div&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;关键模式&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;数据通过 &lt;code&gt;{变量名}&lt;/code&gt;（即 Vue 的 &lt;code&gt;:prop=&quot;变量&quot;&lt;/code&gt;）传入&lt;/li&gt;
&lt;li&gt;事件通过 &lt;code&gt;onClick={回调函数}&lt;/code&gt;（即 Vue 的 &lt;code&gt;@click=&quot;回调&quot;&lt;/code&gt;）传出&lt;/li&gt;
&lt;li&gt;PlayerBar 不知道 Store 的存在——它只是一个数据的搬运工&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;4.3 组件树逐层展开&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;MusicPlayer.svelte  ← 唯一连Store的组件，有subscribe+事件处理+模板
│
├── MiniPlayer.svelte  ← 纯展示，收props传事件。data down, events up
│   └── TrackDisplay.svelte
│       ├── CoverImage.svelte    ← 封面图（播放时旋转，加载时闪动）
│       └── TrackInfo.svelte     ← 歌名+歌手文字
│
├── PlayerBar.svelte  ← 纯展示
│   ├── TrackDisplay.svelte      ← 同上但 expanded 尺寸
│   ├── ProgressControl.svelte   ← 进度条容器
│   │   └── ProgressBar.svelte   ← 实际的进度条div（点击=跳转，拖拽=seek）
│   ├── PlayerControls.svelte    ← 按钮组容器
│   │   ├── ModeButton.svelte    ← 随机/循环模式切换
│   │   ├── PrevButton.svelte    ← 上一首
│   │   ├── PlayButton.svelte    ← 播放/暂停（loading时显示转圈）
│   │   └── NextButton.svelte    ← 下一首
│   └── VolumeControl.svelte     ← 音量控制容器
│       ├── VolumeButton.svelte  ← 音量图标（点一下静音/取消静音）
│       └── VolumeSlider.svelte  ← 音量滑块（拖拽调音量）
│
└── Playlist.svelte  ← 歌单列表
    └── 每个歌单项：封面+歌名+歌手，点击切歌
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;h2&gt;五、完整数据流（用户点&quot;播放&quot;按钮 → 音乐响起）&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;1. 用户点击播放按钮
        │
        ▼
2. PlayButton.svelte 触发 onclick 事件
        │
        ▼
3. PlayerControls.svelte 转发 onPlayClick 事件
        │
        ▼
4. PlayerBar.svelte 转发 onPlayClick 事件
        │
        ▼
5. MusicPlayer.svelte 的 togglePlay() 执行
        │   function togglePlay() { musicPlayerStore.toggle(); }
        ▼
6. musicPlayerStore.toggle() 执行
        │   if (isPlaying) audio.pause(); else audio.play();
        ▼
7. audio.play() 成功 → 浏览器触发 &quot;play&quot; 事件
        │
        ▼
8. setupAudioListeners 中的 &quot;play&quot; 回调执行
        │   this.state.isPlaying = true;
        │   this.broadcastState();
        ▼
9. broadcastState() → 通知所有订阅者
        │
   ┌────┴──────────────┐
   ▼                   ▼
10a. MusicPlayer      10b. MusicFabButton
   state.isPlaying       playerState.isPlaying
   = true                = true
   │                     │
   ▼                     ▼
11a. Svelte自动更新    11b. 按钮图标从 ▶ 变成 ⏸
   PlayButton图标          按钮开始脉冲动画
   进度条开始走动
   封面开始旋转
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;每一步都是单向的&lt;/strong&gt;：用户操作 → 事件上抛 → Store 方法 → Audio API → 浏览器事件 → Store 状态变化 → 广播 → 组件自动刷新。没有任何一步是&quot;组件A直接改了组件B的数据&quot;。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;六、如果你是 Vue 开发者，这个 Store 用 Pinia 怎么写&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;// stores/musicPlayer.ts —— Vue 3 + Pinia 等价实现
import { defineStore } from &apos;pinia&apos;;
import { ref, computed } from &apos;vue&apos;;

export const useMusicPlayerStore = defineStore(&apos;musicPlayer&apos;, () =&amp;gt; {
  // ★ state：用 ref 替代
  const audio = ref&amp;lt;HTMLAudioElement | null&amp;gt;(null);
  const currentSong = ref&amp;lt;Song&amp;gt;({ ...DEFAULT_SONG });
  const playlist = ref&amp;lt;Song[]&amp;gt;([]);
  const currentIndex = ref(0);
  const isPlaying = ref(false);
  const currentTime = ref(0);
  const duration = ref(0);
  const volume = ref(0.7);

  // ★ 初始化：相当于 initialize()
  async function init() {
    audio.value = new Audio();
    setupListeners();
    await loadPlaylist();
  }

  // ★ 监听 Audio 事件
  function setupListeners() {
    audio.value!.addEventListener(&apos;play&apos;, () =&amp;gt; {
      isPlaying.value = true;  // Vue: ref 自动触发响应式更新
    });
    audio.value!.addEventListener(&apos;timeupdate&apos;, () =&amp;gt; {
      currentTime.value = audio.value!.currentTime;
    });
    // ...其他事件同理
  }

  // ★ 操作：直接改 ref
  function toggle() {
    if (isPlaying.value) {
      audio.value!.pause();
    } else {
      audio.value!.play();
    }
  }

  function next() {
    currentIndex.value = (currentIndex.value + 1) % playlist.value.length;
    loadSong(playlist.value[currentIndex.value]);
  }

  return { currentSong, playlist, isPlaying, currentTime, duration,
           volume, init, toggle, next };
});
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;Vue 组件中用&lt;/strong&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;&amp;lt;script setup&amp;gt;
import { useMusicPlayerStore } from &apos;@/stores/musicPlayer&apos;;
import { onMounted, onUnmounted } from &apos;vue&apos;;

const store = useMusicPlayerStore();

onMounted(() =&amp;gt; store.init());
onUnmounted(() =&amp;gt; store.$dispose());
&amp;lt;/script&amp;gt;

&amp;lt;template&amp;gt;
  &amp;lt;!-- 直接读 store 的 ref，不用 subscribe --&amp;gt;
  &amp;lt;PlayButton :playing=&quot;store.isPlaying&quot; @click=&quot;store.toggle()&quot; /&amp;gt;
  &amp;lt;ProgressBar :current=&quot;store.currentTime&quot; :total=&quot;store.duration&quot; /&amp;gt;
&amp;lt;/template&amp;gt;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Vue 的 Pinia 比 Svelte 的 Store 更简单——&lt;code&gt;ref&lt;/code&gt; 自带响应式，组件直接用 &lt;code&gt;store.xxx&lt;/code&gt; 就能自动追踪变化，不需要手动 &lt;code&gt;subscribe/unsubscribe&lt;/code&gt;。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;七、总结：这个播放器设计的四个核心原则&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;原则&lt;/th&gt;
&lt;th&gt;说明&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;单一数据源&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;所有状态存在 &lt;code&gt;musicPlayerStore.state&lt;/code&gt; 里，组件不自己存状态&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;单向数据流&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;事件向上走（组件→回调→Store方法），数据向下走（Store→broadcastState→组件）&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;组件不碰 Store（除顶层外）&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;PlayerBar/MiniPlayer 及以下所有子组件只收 props 和发事件，完全不知道 Store 存在&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;原生 Audio API 在 Store 内部封装&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;所有 &lt;code&gt;audio.play()&lt;/code&gt;、&lt;code&gt;audio.pause()&lt;/code&gt; 都在 Store 方法里，组件不直接操作 DOM 的 Audio&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;hr /&gt;
&lt;blockquote&gt;
&lt;p&gt;读完这篇，你应该能回答：①播放器是怎么拿到歌单的 ②点播放按钮后数据是怎么流动的 ③如果要把这个播放器用 Vue 重写，Store 怎么写。&lt;/p&gt;
&lt;/blockquote&gt;
</content:encoded></item><item><title>前端学习计划——零基础到实战</title><link>https://yuzumoon82.github.io/personal-blog/posts/%E5%89%8D%E7%AB%AF%E5%AD%A6%E4%B9%A0%E8%AE%A1%E5%88%92-%E9%9B%B6%E5%9F%BA%E7%A1%80%E5%88%B0%E5%AE%9E%E6%88%98/</link><guid isPermaLink="true">https://yuzumoon82.github.io/personal-blog/posts/%E5%89%8D%E7%AB%AF%E5%AD%A6%E4%B9%A0%E8%AE%A1%E5%88%92-%E9%9B%B6%E5%9F%BA%E7%A1%80%E5%88%B0%E5%AE%9E%E6%88%98/</guid><description>从 HTML/CSS/JS 基础语法到独立完成完整前端项目</description><pubDate>Thu, 24 Jul 2025 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;前端学习计划 —— 零基础到实战项目&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;已有 10 天基础（HTML/CSS/JS 语法），目标：能独立完成一个完整的前端项目。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;hr /&gt;
&lt;h2&gt;你的起点&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;✅ HTML：标签结构、表单、语义化&lt;/li&gt;
&lt;li&gt;✅ CSS：选择器、盒模型、Flex、定位&lt;/li&gt;
&lt;li&gt;✅ JavaScript：变量、函数、数组、对象、DOM 操作、事件&lt;/li&gt;
&lt;li&gt;❌ 还没做过完整项目&lt;/li&gt;
&lt;li&gt;❌ 还没用过任何框架和工具&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;你的终点（4 周后）&lt;/h2&gt;
&lt;p&gt;能独立完成一个 &lt;strong&gt;个人作品集网站&lt;/strong&gt;，包含：页面布局、交互效果、数据展示、响应式适配，部署到线上可访问。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;第一周：巩固基础 + 第一个静态项目&lt;/h2&gt;
&lt;h3&gt;Day 1：CSS 实战强化 —— 还原一个导航栏&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;学&lt;/strong&gt;：CSS 变量、过渡动画（transition）、伪类（:hover, :active）、box-shadow&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;练&lt;/strong&gt;：手写一个企业官网导航栏&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Logo + 5 个菜单项 + 右侧按钮&lt;/li&gt;
&lt;li&gt;鼠标悬停下拉子菜单&lt;/li&gt;
&lt;li&gt;滚动后导航栏吸顶 + 背景变色&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;不查代码，自己写&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 2：CSS 布局强化 —— 还原一个卡片列表&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;学&lt;/strong&gt;：Grid 布局、object-fit、文本截断（text-overflow: ellipsis）&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;练&lt;/strong&gt;：手写一个商品/文章卡片列表&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;3 列自适应网格布局（PC 3 列 → 平板 2 列 → 手机 1 列）&lt;/li&gt;
&lt;li&gt;卡片含图片、标题、描述（2 行截断）、标签、日期&lt;/li&gt;
&lt;li&gt;卡片悬停上浮 + 阴影效果&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 3：JavaScript DOM 实战 —— 待办事项 Todo List&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;学&lt;/strong&gt;：localStorage 本地存储、事件委托、数组 CRUD&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;练&lt;/strong&gt;：手写一个完整的 Todo List&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;添加、删除、标记完成、编辑&lt;/li&gt;
&lt;li&gt;全部/进行中/已完成 三个筛选 tab&lt;/li&gt;
&lt;li&gt;数据存 localStorage，刷新不丢失&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;这是你的第一个 JS 完整应用，全部手写，不参考&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 4：JavaScript 异步 —— 天气预报查询页&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;学&lt;/strong&gt;：fetch API、async/await、JSON、try/catch&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;练&lt;/strong&gt;：手写一个天气查询页&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;输入城市名 → 调免费天气 API → 展示温度/天气/湿度/风速&lt;/li&gt;
&lt;li&gt;Loading 加载动画&lt;/li&gt;
&lt;li&gt;错误处理（城市不存在、网络错误）&lt;/li&gt;
&lt;li&gt;推荐 API：wttr.in（免费、无需注册，返回 JSON）&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;// 示例：用 fetch 调天气 API
const city = &apos;beijing&apos;;
const response = await fetch(`https://wttr.in/${city}?format=j1`);
const data = await response.json();
console.log(data.current_condition[0].temp_C); // 当前温度
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 5-6：第一个完整项目 —— 个人作品集网站&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Day 5&lt;/strong&gt;：完成页面结构 + 样式&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;头部 Hero 区（头像、姓名、一句话介绍、GitHub 链接）&lt;/li&gt;
&lt;li&gt;关于我（技能标签云）&lt;/li&gt;
&lt;li&gt;项目展示区（3-4 个项目卡片，含截图、描述、技术栈标签、链接）&lt;/li&gt;
&lt;li&gt;时间线（学习/工作经历）&lt;/li&gt;
&lt;li&gt;底部联系方式&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Day 6&lt;/strong&gt;：添加交互 + 部署上线&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;导航平滑滚动&lt;/li&gt;
&lt;li&gt;滚动到项目区域时卡片渐入动画&lt;/li&gt;
&lt;li&gt;回到顶部按钮&lt;/li&gt;
&lt;li&gt;用 &lt;strong&gt;GitHub Pages&lt;/strong&gt; 免费部署，生成一个可访问的网址&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;这 6 天做完，你就有了第一个能给别人看的线上作品。不要追求完美，先跑通。&lt;/strong&gt;&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;第二周：工程化入门 + 框架起步&lt;/h2&gt;
&lt;h3&gt;Day 7：认识 Git + Node.js&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;学&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Git：init、add、commit、push、clone&lt;/li&gt;
&lt;li&gt;Node.js：是什么、npm 是什么、&lt;code&gt;npm install&lt;/code&gt; 做了什么&lt;/li&gt;
&lt;li&gt;用 VS Code 打开终端，跑 &lt;code&gt;node -v&lt;/code&gt; 和 &lt;code&gt;npm -v&lt;/code&gt; 确认安装&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;练&lt;/strong&gt;：把第一周的作品集项目用 Git 管起来，推到 GitHub&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 8：模块化 + 打包工具概念&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;学&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;为什么需要模块化（一个 HTML 里引 10 个 &lt;code&gt;&amp;lt;script&amp;gt;&lt;/code&gt; 的痛苦）&lt;/li&gt;
&lt;li&gt;import/export 语法&lt;/li&gt;
&lt;li&gt;Vite 是什么（5 分钟理解：它帮你打包、热更新、处理兼容）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;练&lt;/strong&gt;：用 Vite 创建一个项目&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;npm create vite@latest my-project -- --template vanilla
cd my-project
npm install
npm run dev
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;把第一周的 Todo List 迁移到 Vite 项目中，拆分成多个 JS 文件用 import/export 组织。&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 9-10：Vue 3 入门（选 Vue 因为上手最快）&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Day 9 学&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Vue 是什么（响应式数据 + 组件化）&lt;/li&gt;
&lt;li&gt;单文件组件（.vue 文件：template + script + style 在一块）&lt;/li&gt;
&lt;li&gt;ref、computed、v-if、v-for、v-on、v-bind&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Day 9 练&lt;/strong&gt;：用 Vue 3 重写 Todo List&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;npm create vite@latest vue-todo -- --template vue
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;把之前原生 JS 写的 Todo List 改成 Vue 组件：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;App.vue&lt;/code&gt; → 主布局&lt;/li&gt;
&lt;li&gt;&lt;code&gt;TodoList.vue&lt;/code&gt; → 列表展示&lt;/li&gt;
&lt;li&gt;&lt;code&gt;TodoItem.vue&lt;/code&gt; → 单条待办&lt;/li&gt;
&lt;li&gt;&lt;code&gt;AddTodo.vue&lt;/code&gt; → 输入框 + 添加按钮&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Day 10 练&lt;/strong&gt;：用 Vue 3 重写天气查询页&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;拆分为 &lt;code&gt;SearchBox.vue&lt;/code&gt; + &lt;code&gt;WeatherCard.vue&lt;/code&gt; + &lt;code&gt;Loading.vue&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;数据用 ref 管理&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;第三周：Vue 深入 + 路由 + 组件库&lt;/h2&gt;
&lt;h3&gt;Day 11：组件通信&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;学&lt;/strong&gt;：props（父→子）、emit（子→父）、provide/inject（跨层级）&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;练&lt;/strong&gt;：在 Todo List 里补一个全局主题切换（亮色/暗色），用 provide/inject 传递主题状态&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 12：Vue Router 路由&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;学&lt;/strong&gt;：vue-router、&lt;code&gt;&amp;lt;router-view&amp;gt;&lt;/code&gt;、&lt;code&gt;&amp;lt;router-link&amp;gt;&lt;/code&gt;、动态路由&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;练&lt;/strong&gt;：把作品集网站拆成多页面&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;/&lt;/code&gt; → 首页（Hero + 关于我）&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/projects&lt;/code&gt; → 项目列表页&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/projects/:id&lt;/code&gt; → 项目详情页&lt;/li&gt;
&lt;li&gt;&lt;code&gt;/timeline&lt;/code&gt; → 时间线页&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 13：组件库 + 响应式实战&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;学&lt;/strong&gt;：Element Plus（Vue 3 最流行的组件库）&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;表格、表单、对话框、消息提示&lt;/li&gt;
&lt;li&gt;不需要自己写轮子，学会看文档用现成组件&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;练&lt;/strong&gt;：在作品集项目里加一个留言板页面&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;用 Element Plus 的表单组件（姓名、邮箱、留言内容、提交按钮）&lt;/li&gt;
&lt;li&gt;用表格组件展示留言列表&lt;/li&gt;
&lt;li&gt;留言数据存 localStorage&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 14：完整 Vue 项目 —— 豆瓣电影搜索&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;项目&lt;/strong&gt;：做一个电影搜索 + 收藏网站&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;搜索框 → 调豆瓣/OMDb API → 展示电影卡片（海报、片名、年份、评分）&lt;/li&gt;
&lt;li&gt;点击卡片 → 电影详情页（剧情简介、演员、评分）&lt;/li&gt;
&lt;li&gt;收藏功能（收藏列表存 localStorage）&lt;/li&gt;
&lt;li&gt;用 Vue Router + Element Plus&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;做完这一周，你就有了一个用框架做的完整单页应用。&lt;/strong&gt;&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;第四周：高阶 + 简历项目&lt;/h2&gt;
&lt;h3&gt;Day 15-16：状态管理 Pinia + API 封装&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;学&lt;/strong&gt;：Pinia（Vue 的状态管理库）、axios 封装（拦截器、统一错误处理）&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;练&lt;/strong&gt;：把豆瓣电影搜索项目的全局状态（收藏列表、用户偏好）迁移到 Pinia store&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 17-18：简历项目 —— 个人博客/知识库&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;项目定位&lt;/strong&gt;：这是你面试时要讲的&quot;核心项目&quot;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;功能&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;文章列表页（分页、标签筛选、搜索）&lt;/li&gt;
&lt;li&gt;文章详情页（Markdown 渲染）&lt;/li&gt;
&lt;li&gt;暗色/亮色主题切换&lt;/li&gt;
&lt;li&gt;响应式适配&lt;/li&gt;
&lt;li&gt;GitHub Pages 部署&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;技术栈&lt;/strong&gt;：Vue 3 + Vue Router + Pinia + Element Plus + Vite&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;Day 19-20：完善 + 面试准备&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;优化加载速度（图片懒加载、路由懒加载）&lt;/li&gt;
&lt;li&gt;写 README.md（项目介绍、技术栈、如何运行、截图）&lt;/li&gt;
&lt;li&gt;补充代码注释&lt;/li&gt;
&lt;li&gt;把 3 个项目（作品集、电影搜索、个人博客）的 GitHub 链接更新到简历&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;你的项目矩阵（面试时能讲的）&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;项目&lt;/th&gt;
&lt;th&gt;技术栈&lt;/th&gt;
&lt;th&gt;难度&lt;/th&gt;
&lt;th&gt;完成时间&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;个人作品集网站&lt;/td&gt;
&lt;td&gt;HTML + CSS + JS 原生&lt;/td&gt;
&lt;td&gt;⭐&lt;/td&gt;
&lt;td&gt;Day 6&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;天气预报查询&lt;/td&gt;
&lt;td&gt;fetch + async/await&lt;/td&gt;
&lt;td&gt;⭐&lt;/td&gt;
&lt;td&gt;Day 4&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Todo List&lt;/td&gt;
&lt;td&gt;Vue 3 + 组件通信&lt;/td&gt;
&lt;td&gt;⭐⭐&lt;/td&gt;
&lt;td&gt;Day 10&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;豆瓣电影搜索&lt;/td&gt;
&lt;td&gt;Vue Router + Element Plus&lt;/td&gt;
&lt;td&gt;⭐⭐⭐&lt;/td&gt;
&lt;td&gt;Day 14&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;个人博客/知识库&lt;/td&gt;
&lt;td&gt;Vue 3 全家桶 + Pinia&lt;/td&gt;
&lt;td&gt;⭐⭐⭐⭐&lt;/td&gt;
&lt;td&gt;Day 18&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;hr /&gt;
&lt;h2&gt;学习原则&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;先跑通，再优化&lt;/strong&gt;。不要追求完美代码，先让页面能看、功能能用&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;每天一个完整小产出&lt;/strong&gt;。哪怕只是一个导航栏，也要做完、能看&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;遇到问题先自己调试 15 分钟&lt;/strong&gt;。console.log 是最好用的工具&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;不抄完整代码&lt;/strong&gt;。看思路、自己写、卡住了再看答案&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;GitHub 每天提交&lt;/strong&gt;。绿点就是你的学习轨迹&lt;/li&gt;
&lt;/ol&gt;
&lt;hr /&gt;
&lt;blockquote&gt;
&lt;p&gt;4 周前你只会 HTML 标签。4 周后你有 3-5 个完整项目、能独立开发 Vue 单页应用、简历上有东西可写。开始吧。&lt;/p&gt;
&lt;/blockquote&gt;
</content:encoded></item><item><title>MCP 核心链路面试话术</title><link>https://yuzumoon82.github.io/personal-blog/posts/mcp%E4%B8%89%E6%AD%A5%E8%B5%B0-%E9%9D%A2%E8%AF%95%E8%AF%9D%E6%9C%AF/</link><guid isPermaLink="true">https://yuzumoon82.github.io/personal-blog/posts/mcp%E4%B8%89%E6%AD%A5%E8%B5%B0-%E9%9D%A2%E8%AF%95%E8%AF%9D%E6%9C%AF/</guid><description>Transport → McpSyncClient → ToolCallback，从架构到代码，讲透 MCP 三步走</description><pubDate>Wed, 23 Jul 2025 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;MCP 核心链路面试话术：Transport → McpSyncClient → ToolCallback&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;约 5000 字，面向技术面试。从架构痛点 → 设计原理 → 代码实现 → 生产实践，四个维度讲透 MCP 三步走模式。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;hr /&gt;
&lt;h2&gt;一、开场定调：MCP 解决了什么问题（30 秒，100 字）&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;话术&lt;/strong&gt;：&lt;/p&gt;
&lt;p&gt;&quot;MCP（Model Context Protocol）是一个标准化的工具接入协议。它让 LLM Agent 接入外部工具这件事从&apos;每个工具写一套适配代码&apos;变成&apos;只要支持 MCP 协议，接上去就能用&apos;。类比就是 USB-C——你不用关心设备内部的电路，只要接口匹配，插上就工作。&lt;/p&gt;
&lt;p&gt;在我们的项目里，从 Transport 创建到最终注入 Agent，有一个贯穿始终的三步走模式，我来完整讲一下。&quot;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;考察点&lt;/strong&gt;：是否一句话说清 MCP 的定位（协议，不是框架），是否有类比能力。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;二、总览：三步走模式是什么（1 分钟，300 字）&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;话术&lt;/strong&gt;：&lt;/p&gt;
&lt;p&gt;&quot;整个链路的入口只有一个模式，我叫它三步走：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第一步：创建 Transport&lt;/strong&gt;——选择你用什么方式连接到 MCP Server。MCP 协议定义了三种标准 Transport：Stdio（子进程通信）、SSE（HTTP 长连接）、Streamable HTTP（标准 REST 短连接）。这一步要回答的问题是：&apos;我的 Agent 和工具 Server 之间，通过什么通道通信？&apos;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第二步：创建 McpSyncClient 并 initialize&lt;/strong&gt;——这是 MCP 协议的握手层。把 Transport 传给 &lt;code&gt;McpClient.sync(transport).build()&lt;/code&gt; 得到一个 Client 对象，然后调用 &lt;code&gt;initialize()&lt;/code&gt;。这一步做了两件事：协议版本协商和工具列表发现。握手成功后，Client 内部缓存了 Server 的元信息和所有可用工具的完整 Schema。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第三步：包装为 ToolCallback[]&lt;/strong&gt;——这是 MCP 协议和 Spring AI 框架之间的适配层。&lt;code&gt;SyncMcpToolCallbackProvider&lt;/code&gt; 遍历 Client 持有的工具列表，把每个 &lt;code&gt;McpSchema.Tool&lt;/code&gt; 包装为一个 &lt;code&gt;SyncMcpToolCallback&lt;/code&gt;，最终暴露为 Spring AI 标准的 &lt;code&gt;ToolCallback[]&lt;/code&gt;，注入 &lt;code&gt;ChatClient&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;这三步做完，Agent 调用工具的全链路就通了：LLM 输出 tool_call → ChatClient 匹配 ToolCallback → ToolCallback 调用 McpSyncClient → Transport 发送 JSON-RPC → MCP Server 执行 → 原路返回结果。&lt;/p&gt;
&lt;p&gt;我用代码来展开每一步。&quot;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;考察点&lt;/strong&gt;：是否有全局视角，能否用三层结构讲清复杂链路。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;三、第一步深入：Transport —— 三种通信方式的选型逻辑（1.5 分钟，800 字）&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;话术&lt;/strong&gt;：&lt;/p&gt;
&lt;p&gt;&quot;Transport 是 MCP 架构的第一层抽象，它要解决的问题是：Agent 和工具 Server 可能部署在不同的机器上，可能用不同的通信协议，可能跑在不同的操作系统上。MCP 把所有这些差异抽象成了三种标准 Transport。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第一种，StdioClientTransport——子进程通信。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;最直接的方式。Agent 通过 &lt;code&gt;ProcessBuilder&lt;/code&gt; 启动工具 Server 的 JAR 包作为子进程，然后通过子进程的 stdin 发 JSON-RPC 请求，通过 stdout 接收响应。Server 的生命周期和子进程绑定。&lt;/p&gt;
&lt;p&gt;代码上就是两步：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ServerParameters params = ServerParameters.builder(&quot;java&quot;)
    .args(&quot;-jar&quot;, &quot;mcp-server-stdio.jar&quot;).build();
StdioClientTransport transport = new StdioClientTransport(params);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Stdio 的特点是：零网络开销，天然安全（进程隔离），但 Server 和 Agent 必须在同一台机器上。适合本地开发和单机部署。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第二种，HttpClientSseClientTransport——SSE 长连接。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;Server 作为独立的 HTTP 服务部署。Agent 和 Server 之间维持一个 SSE（Server-Sent Events）长连接，Server 可以主动推送事件给 Agent。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;HttpClientSseClientTransport transport = HttpClientSseClientTransport
    .builder(&quot;http://127.0.0.1:8003&quot;)
    .sseEndpoint(&quot;/sse&quot;)
    .build();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;SSE 的好处是支持服务端主动推送——比如工具执行进度、日志流。代价是必须维护长连接。网络断开后 Agent 需要感知到并自动重连，否则后续的工具调用全部失败。我们专门写了一个 &lt;code&gt;RetrySSEMcpService&lt;/code&gt; 来做心跳检测和自动重连——每 5 秒 ping 一次，失败了就 CAS 互斥地启动重连线程，重连成功后必须重建 ChatClient，因为旧的 ToolCallback 持有的 McpSyncClient 引用已经失效了。这是一个很容易踩的坑。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第三种，HttpClientStreamableHttpTransport——标准 HTTP 短连接。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;我们生产环境实际用的就是这种。它最接近传统 REST API，每次工具调用就是一个独立的 HTTP POST 请求。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;HttpClientStreamableHttpTransport transport = HttpClientStreamableHttpTransport
    .builder(&quot;https://api.tavily.com/mcp&quot;)
    .requestBuilder(r -&amp;gt; r.header(&quot;Authorization&quot;, &quot;Bearer &quot; + apiKey))
    .build();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;为什么选它？三个原因：第一，认证友好。我们通过 &lt;code&gt;requestBuilder&lt;/code&gt; 直接把 API Key 注入到 HTTP Header，网关层可以统一做鉴权。第二，运维简单。没有长连接，不需要负载均衡器维护连接亲和性，Server 重启不影响已经在队列里的请求——它们收到 503 后会重试。第三，和 API 网关天然兼容。限流、鉴权、日志采集这些基础设施全部复用，不需要为 MCP 单独开一条通道。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;选型总结&lt;/strong&gt;：选 Transport 的本质是匹配部署场景。Stdio 匹配本地，SSE 匹配需要推送的远程，Streamable HTTP 匹配需要认证和网关的标准云服务。代码逻辑完全一样，换 Transport 只是换一个工厂方法。&quot;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;考察点&lt;/strong&gt;：是否亲手用过三种 Transport、是否有选型思考框架、是否踩过生产环境的坑（长连接断开 + 重连机制）。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;四、第二步深入：McpSyncClient.initialize() —— 握手机制（1 分钟，500 字）&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;话术&lt;/strong&gt;：&lt;/p&gt;
&lt;p&gt;&quot;Transport 建好后，下一步就是用 Transport 创建 McpSyncClient 并握手。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;McpSyncClient client = McpClient.sync(transport)
    .requestTimeout(Duration.ofSeconds(300))
    .build();
client.initialize();  // ← 握手
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这里有一个非常重要的设计决策：&lt;strong&gt;initialize() 只在应用启动时调用一次，不在每次请求时调用&lt;/strong&gt;。为什么？因为 initialize 内部做了两件事，都有网络开销。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第一件事：协议版本协商。&lt;/strong&gt; Agent 和 Server 交换各自的身份信息和能力声明。Agent 说&apos;我是 dodo-agent v1.0，我支持 tools 能力&apos;，Server 回应&apos;我是 tavily-server v1.0，我支持 tools 能力，以下是我的工具列表&apos;。这一步本质上是一个 HTTP 往返。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第二件事：工具列表拉取。&lt;/strong&gt; Client 内部会自动调用 &lt;code&gt;tools/list&lt;/code&gt; 获取 Server 暴露的所有工具。每个工具的返回包括它的名称、自然语言描述、参数 JSON Schema——这三个字段是 LLM 决定要不要调、怎么调这个工具的全部依据。&lt;/p&gt;
&lt;p&gt;握手成功后，Client 在内存中缓存了 serverInfo 和 toolList。后续 &lt;code&gt;client.callTool()&lt;/code&gt; 不再需要握手，直接发起 JSON-RPC 调用。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;如果在每次请求时都握手会怎样？&lt;/strong&gt; 假设一次 MCP 握手耗时 100ms，那每次用户请求都会额外增加 100ms 的首字响应延迟。而且这是纯浪费——工具的 Schema 不会在两次请求之间发生变化。所以我们把 initialize 放在 &lt;code&gt;@PostConstruct&lt;/code&gt; 或 &lt;code&gt;InitializingBean.afterPropertiesSet()&lt;/code&gt; 中，启动时执行一次，缓存结果，请求间复用。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;这个缓存是线程安全的吗？&lt;/strong&gt; 是。McpSyncClient 的 &lt;code&gt;callTool()&lt;/code&gt; 底层用的是 Java 11 的 &lt;code&gt;HttpClient&lt;/code&gt;，它内部的连接池是线程安全的。ToolCallback 是只读对象，天然线程安全。所以我们可以在高并发下放心地用同一个 client 实例。&quot;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;考察点&lt;/strong&gt;：是否理解 initialize 做了什么、为什么只调一次、是否考虑过并发安全性。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;五、第三步深入：ToolCallback[] —— 从 MCP 协议到 Spring AI 框架的适配（1 分钟，500 字）&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;话术&lt;/strong&gt;：&lt;/p&gt;
&lt;p&gt;&quot;握完手后，McpSyncClient 内部已经有了 &lt;code&gt;List&amp;lt;McpSchema.Tool&amp;gt;&lt;/code&gt;。但这些是 MCP 协议层的对象，Spring AI 不认识。我们需要一个适配层把它们转换成 Spring AI 的 &lt;code&gt;ToolCallback&lt;/code&gt; 接口。&lt;/p&gt;
&lt;p&gt;这就是 &lt;code&gt;SyncMcpToolCallbackProvider&lt;/code&gt; 做的事情：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;List&amp;lt;McpSyncClient&amp;gt; mcpClients = List.of(client);

SyncMcpToolCallbackProvider provider = SyncMcpToolCallbackProvider.builder()
    .mcpClients(mcpClients)
    .toolFilter((conn, tool) -&amp;gt; tool.name().startsWith(&quot;goods&quot;))
    .build();

ToolCallback[] callbacks = provider.getToolCallbacks();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这个 Provider 做了三件事：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第一，遍历。&lt;/strong&gt; 遍历你给它的所有 McpSyncClient。注意，&lt;code&gt;mcpClients&lt;/code&gt; 是一个 List，意味着你可以同时连多个 MCP Server——比如 Tavily 搜索、飞书文档、MySQL 查询——一个 Provider 把它们的工具列表合并为一个统一的 ToolCallback[]。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第二，过滤。&lt;/strong&gt; &lt;code&gt;toolFilter&lt;/code&gt; 是一个可选的 Predicate。比如你的 Agent 只需要搜索工具，可以用 &lt;code&gt;toolFilter&lt;/code&gt; 把其他工具过滤掉。这在多租户场景下非常有用——不同租户可以看到不同的工具子集，而不需要为每个租户部署一套 Server。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第三，包装。&lt;/strong&gt; 对每个 &lt;code&gt;McpSchema.Tool&lt;/code&gt;，创建一个 &lt;code&gt;SyncMcpToolCallback&lt;/code&gt;。这个 Callback 实现了 Spring AI 的 &lt;code&gt;ToolCallback&lt;/code&gt; 接口，核心方法就是 &lt;code&gt;call(argsJson)&lt;/code&gt;。内部做的事很简单：把 argsJson 转成 &lt;code&gt;McpSchema.CallToolRequest&lt;/code&gt;，调 &lt;code&gt;McpSyncClient.callTool(request)&lt;/code&gt;，返回结果。&lt;/p&gt;
&lt;p&gt;然后把 ToolCallback[] 注入 ChatClient：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ChatClient chatClient = ChatClient.builder(chatModel)
    .defaultToolCallbacks(callbacks)
    .build();
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;之后每次 LLM 请求，Spring AI 会自动把这些 Callback 的 Schema 作为 available tools 参数发送给 LLM。LLM 返回 tool_call 时，框架根据 name 匹配到对应的 Callback，调用 &lt;code&gt;call()&lt;/code&gt; 方法，整个链路就跑通了。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;为什么这一层叫适配层？&lt;/strong&gt; 因为它的职责就是解耦——MCP Server 不关心 Agent 用的是什么框架（Spring AI、LangChain、LlamaIndex），Agent 框架也不关心工具 Server 是什么语言实现的。Connector 负责在两者之间做翻译。这是经典的设计模式——适配器模式。&quot;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;考察点&lt;/strong&gt;：是否理解 CallbackProvider 的设计意图、是否能讲出多 Server 合并和过滤的能力。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;六、全链路串联：从 LLM 的 tool_call 到工具执行结果返回（1 分钟，600 字）&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;话术&lt;/strong&gt;：&lt;/p&gt;
&lt;p&gt;&quot;三步走完后，Agent 的工具调用链路就完整了。我来串一下一次实际调用的全流程。&lt;/p&gt;
&lt;p&gt;用户问&apos;今天北京天气怎么样&apos;，我们来看看数据是怎么流的：&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第 1 步：LLM 分析 + tool_call 生成。&lt;/strong&gt; ChatClient 把用户问题 + 聊天历史 + 所有可用工具的 Schema 发给 LLM。LLM 看到有一个 &lt;code&gt;getWeather&lt;/code&gt; 工具，参数是 &lt;code&gt;city&lt;/code&gt;，描述是&apos;根据城市名称查询天气&apos;，于是它判断：这个问题需要调用这个工具。LLM 返回一个 tool_call：&lt;code&gt;{name: &quot;getWeather&quot;, arguments: {city: &quot;北京&quot;}}&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第 2 步：ChatClient 匹配 ToolCallback。&lt;/strong&gt; ChatClient 收到 tool_call，通过 name &lt;code&gt;getWeather&lt;/code&gt; 在 ToolCallback[] 中找到对应的 &lt;code&gt;SyncMcpToolCallback&lt;/code&gt;——这一步是纯内存操作，毫秒级。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第 3 步：ToolCallback.call(argsJson)。&lt;/strong&gt; &lt;code&gt;SyncMcpToolCallback&lt;/code&gt; 内部用 argsJson 构造出 &lt;code&gt;McpSchema.CallToolRequest&lt;/code&gt;，调用 &lt;code&gt;McpSyncClient.callTool(request)&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第 4 步：McpSyncClient 走 Transport。&lt;/strong&gt; Client 把 CallToolRequest 序列化为 JSON-RPC 2.0 格式，通过 Transport 发送 HTTP POST 到 MCP Server 的端点。如果是 Streamable HTTP，这就是一个标准 POST 请求；如果是 SSE，这是一个独立的 HTTP 请求（不经过 SSE 长连接）。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第 5 步：MCP Server 反射执行。&lt;/strong&gt; Server 收到 JSON-RPC 请求，解析出 method 是 &lt;code&gt;tools/call&lt;/code&gt;，name 是 &lt;code&gt;getWeather&lt;/code&gt;。通过反射找到 &lt;code&gt;WeatherService.getWeather(&quot;北京&quot;)&lt;/code&gt; 方法，执行，拿到返回值 &lt;code&gt;&quot;北京: 晴, 25°C&quot;&lt;/code&gt;，包装为 &lt;code&gt;McpSchema.CallToolResult&lt;/code&gt;，序列化为 JSON-RPC 响应，原路返回。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第 6 步：结果回到 Agent。&lt;/strong&gt; ToolCallback 把 CallToolResult 的内容提取出来，包装为 &lt;code&gt;ToolResponseMessage&lt;/code&gt;，追加到 LLM 的 messages 列表。ChatClient 把这个结果送回 LLM。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第 7 步：LLM 生成最终回答。&lt;/strong&gt; LLM 现在有了工具执行的结果，基于它生成自然语言回答：&apos;根据最新数据，今天北京天气晴朗，气温 25°C，适合出行。&apos;&lt;/p&gt;
&lt;p&gt;这 7 步中，前两步和最后两步是 Spring AI 框架层，中间三步是 MCP 协议层。框架层和协议层之间的边界就是 ToolCallback——它既是 Spring AI 的 ToolCallback 实现，又持有一个 MCP Client 引用。这就是适配器模式在真实项目中的体现。&quot;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;考察点&lt;/strong&gt;：是否能从单个请求的视角讲清全链路、是否理解每层的职责边界。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;七、生产实践：初始化时机 + 超时 + 健壮性（1 分钟，500 字）&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;话术&lt;/strong&gt;：&lt;/p&gt;
&lt;p&gt;&quot;三步走模式说完了，我再补充三个生产环境的实践细节。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第一个，初始化时机的选择。&lt;/strong&gt; 我们在 &lt;code&gt;AgentController&lt;/code&gt; 的 &lt;code&gt;afterPropertiesSet()&lt;/code&gt; 中执行整个三步走流程，把得到的 &lt;code&gt;ToolCallback[]&lt;/code&gt; 缓存到成员变量。每次 HTTP 请求进来，创建一个新的 Agent 实例，注入同一个 &lt;code&gt;ToolCallback[]&lt;/code&gt;。这意味着 MCP 握手只在应用启动时执行一次，所有请求共享。ToolCallback[] 是只读的，线程安全，McpSyncClient 下的 HTTP 连接池也是线程安全的。但如果你的 MCP Server 支持热更新工具列表，这种缓存策略就不适用了——你需要定期刷新 ToolCallback[]。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第二个，超时配置。&lt;/strong&gt; 不同工具的执行时间差异很大。天气查询可能 50ms 返回，搜索引擎可能要 5 秒，大数据分析可能要几分钟。我们在创建 McpSyncClient 时用 &lt;code&gt;requestTimeout&lt;/code&gt; 设置硬超时：&lt;code&gt;Duration.ofSeconds(300)&lt;/code&gt;。这是 5 分钟，足够覆盖搜索引擎的极端情况。但要注意——这个超时是整个工具调用的超时，不是单次 HTTP 的超时。如果 MCP Server 内部有重试逻辑，需要把重试的时间也考虑进去。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;第三个，SSE 长连接的健壮性。&lt;/strong&gt; Streamable HTTP 不需要特别处理，因为每次调用是独立的短连接。但 SSE 不同——它是一个长连接，断开后必须重连。我们的 &lt;code&gt;RetrySSEMcpService&lt;/code&gt; 做到了三点：心跳检测（5 秒一次 ping）、原子重连（AtomicBoolean CAS 保证只有一个线程重连）、全链路刷新（不只重建 McpSyncClient，还要重建 ToolCallback 和 ChatClient）。第三点很容易漏——你重建了底层 Client，但上层 Agent 持有的 ChatClient 还在用旧的 ToolCallback，而旧的 ToolCallback 内部引用的是已经断了连接的 Client。所以重连后必须全链路刷新，一个都不能漏。&quot;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;考察点&lt;/strong&gt;：是否考虑过生产环境的具体问题（启动时初始化、超时配置、长连接断开）。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;八、总结收尾：三步走模式的本质（30 秒，200 字）&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;话术&lt;/strong&gt;：&lt;/p&gt;
&lt;p&gt;&quot;总结一下，Transport → McpSyncClient(+initialize) → ToolCallback[] 这个三步走模式，本质上是用三个抽象层解耦了一个复杂问题：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Transport 层&lt;/strong&gt;：解耦通信方式。换部署环境只需换一个 Transport 工厂方法。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;McpSyncClient 层&lt;/strong&gt;：解耦协议细节。只需调用 &lt;code&gt;callTool()&lt;/code&gt;，不关心 JSON-RPC 怎么编解码。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;ToolCallback 层&lt;/strong&gt;：解耦 Agent 框架。Spring AI 不需要知道工具是 MCP 接的还是本地 &lt;code&gt;@Tool&lt;/code&gt; 注解的。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这三层合在一起，让 Agent 的工具接入从&apos;每个工具写一套代码&apos;变成&apos;支持 MCP 协议即插即用&apos;。这就是 MCP 的核心价值。&quot;&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;九、面试追问预案&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;追问&lt;/th&gt;
&lt;th&gt;回答要点&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;MCP 和 Function Calling 的关系？&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;MCP 负责工具发现和调用（协议层），Function Calling 负责 LLM 决策（能力层）。MCP 把工具 Schema 送到 LLM 面前，Function Calling 让 LLM 决定要不要用、怎么用&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;initialize 握手具体交换了什么？&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;Client 发 &lt;code&gt;protocolVersion&lt;/code&gt; + &lt;code&gt;clientInfo&lt;/code&gt; + &lt;code&gt;capabilities&lt;/code&gt;；Server 返回 &lt;code&gt;serverInfo&lt;/code&gt; + &lt;code&gt;capabilities&lt;/code&gt;。握手成功后 Client 内部自动调 &lt;code&gt;tools/list&lt;/code&gt; 获取完整工具 Schema&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;性能开销多大？&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;握手：1 次 HTTP 往返 + 1 次 tools/list，毫秒级，只在启动时执行。调用：每次 tool_call 多一层 JSON-RPC 包装，开销可忽略。瓶颈在工具执行本身&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;为什么不用 gRPC？&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;gRPC 需要共享 &lt;code&gt;.proto&lt;/code&gt; 文件做代码生成。MCP 在握手时动态发现工具 Schema，松耦合，且 JSON 对人类可调试&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;多个 MCP Server 怎么合并工具？&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;&lt;code&gt;SyncMcpToolCallbackProvider&lt;/code&gt; 的 &lt;code&gt;mcpClients&lt;/code&gt; 是 List，遍历所有 Client 的 toolList，合并为一个 ToolCallback[]，LLM 看到的是一份统一列表&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;工具 Schema 变更了怎么办？&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;缓存策略下需要重启应用或实现定时刷新。如果对可用性要求高，可以实现类似 &lt;code&gt;RetrySSEMcpService&lt;/code&gt; 的定期重建机制&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;hr /&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;最后更新：&lt;/strong&gt; 2026-07-23&lt;/p&gt;
&lt;/blockquote&gt;
</content:encoded></item></channel></rss>